# دليل المساهمة (Contributing Guidelines)

مرحباً بك في مشروع **"المعلم العربي المحترف" (Pro Arab Teacher LMS)**. نرحب بمساهمات المقترحات والتطوير من قبل المجتمع والمطورين.

---

## 📋 قواعد وطريقة المساهمة (How to Contribute)

1. **إعادة استخدام وإصلاح المكونات (Component Standard):**
   - يلتزم المشروع بالتصميم العربي الملكي الأكاديمي (`#1A365D` الأزرق الكحلي مع `#C5A021` الذهبي).
   - استخدام أيقونات `lucide-react` حصرية لتوحيد التصميم.
   - دعم التجاوب الشامل على جميع الشاشات (الهاتف، اللوح، وسطح المكتب).

2. **الأمان وطلبات الذكاء الاصطناعي (AI Requests & Security):**
   - يمنع منعاً باتاً استدعاء API Keys للذكاء الاصطناعي مباشرة من المتصفح (Client-side).
   - جميع استدعاءات Gemini تمر عبر مسارات الخادم في `server.ts` تحت البادئة `/api/*`.

3. **الترجمة واللغات (i18n & Regional Curricula):**
   - عدم وضع نصوص ثابتة مقتطعة داخل المكونات دون إضافتها في `/src/i18n/translations.ts`.
   - مراعاة دعم اتجاه النص `dir="rtl"` للغة العربية و`dir="ltr"` للغة الإنجليزية.
   - إضافة أي مناهج عربية جديدة في `/src/data/regionalCurricula.ts`.

4. **فحص الجودة والتأكد من البناء (Build & Verification):**
   - تشغيل `npm run lint` للتأكد من خلو المشروع من أخطاء TypeScript.
   - تشغيل `npm run build` للتأكد من سلامة ملف التجميع وإصدار الإنتاج.

---

شكراً لمساهمتك في تطوير حلول تقنية عربية سيادية لخدمة التعليم العربي!
